# ── 密钥 / 配置 ──
backend/system_config.json
backend/system_config.json.bak
backend/system_config.json.tmp
.env
**/.env

# ── VS Code（每台机器独立，不共享）──
.vscode/

# ── Python ──
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# ── pip 缓存（升级时可能产生）──
pip/
.pip/
**/pip/
**/.pip/

# ── Node ──
node_modules/

# ── 运行时 ──
LogFiles/
temp_uploads/
# 数据自愈一次性清理的整行留底(含成绩原文, 不入库)
db_backups/
-w

# ── 数据库 ──
backend/smartkb.db

# ── 在线升级运行时文件 ──
.upgrade_lock
.upgrade_state.json
.upgrade_backups/
backend/smartkb.db-shm
backend/smartkb.db-wal
backend/questions.db
*.db

# ── 测试（不提交/同步） ──
tests/
pytest.ini
.pytest_cache/
*.pytest_cache
**/__pycache__/

# ── 开发工具脚本（不部署） ──
scripts/
build-desktop.bat

# ── 覆盖率报告 ──
htmlcov/
.coverage
coverage.xml
.coverage.*
*.cov
*.lcov

# ── IIS 部署配置（各环境不同，不参与版本管理）──
web.config

# ── 在线升级备份 ──
.upgrade_backups/
.upgrade_state.json

# ── 运行时数据 ──
**/ChatHistory/
**/Summary/
stu/
root/

# ── 桌面版（构建产物 + 源码，整目录排除） ──
desktop/
.venv/
youufis/
Temp/
question_media/

# ── 开发期验证产物（活动重置功能调试用） ──
.playwright-mcp/
backend/logs/
# 实施决策日志只留本地，不随仓库分发
docs/
