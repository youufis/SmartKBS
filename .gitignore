# ── 密钥 / 配置 ──
backend/system_config.json
.env
**/.env

# ── VS Code（每台机器独立，不共享）──
.vscode/

# ── Python ──
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# ── pip 缓存（升级时可能产生）──
pip/
.pip/
**/pip/
**/.pip/

# ── Node ──
node_modules/

# ── 运行时 ──
LogFiles/
temp_uploads/
-w

# ── 数据库 ──
backend/smartkb.db

# ── 在线升级运行时文件 ──
.upgrade_lock
.upgrade_state.json
.upgrade_backups/
backend/smartkb.db-shm
backend/smartkb.db-wal
backend/questions.db
*.db

# ── 测试（不提交/同步） ──
tests/
pytest.ini
.pytest_cache/
*.pytest_cache
**/__pycache__/

# ── 覆盖率报告 ──
htmlcov/
.coverage
coverage.xml
.coverage.*
*.cov
*.lcov

# ── IIS 部署配置（各环境不同，不参与版本管理）──
web.config

# ── 在线升级备份 ──
.upgrade_backups/
.upgrade_state.json

# ── 运行时数据 ──
**/ChatHistory/
**/Summary/
stu/
root/
youufis/
Temp/
question_media/
